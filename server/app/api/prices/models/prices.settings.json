{
  "kind": "collectionType",
  "collectionName": "prices",
  "info": {
    "name": "prices",
    "description": ""
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": false
  },
  "attributes": {
    "product": {
      "via": "price",
      "model": "products"
    },
    "active": {
      "type": "boolean",
      "default": true
    },
    "unit_amount": {
      "type": "biginteger",
      "required": true
    },
    "type": {
      "type": "enumeration",
      "enum": [
        "one_time",
        "recurring"
      ],
      "default": "one_time"
    }
  }
}
